pyserial>=3.5
certifi>=2024.2.2
# Pillow 12.0+ switched its build system to pybind11, which the
# Flatpak build can't pull (--no-build-isolation). Cap below 12 so
# we get a wheel-installable sdist or prebuilt wheel.
Pillow>=10.0.0,<12.0